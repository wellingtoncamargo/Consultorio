Skill: create_maui_page (merged)

Objetivo:
Gerar página MAUI (XAML + ViewModel) para CRUD de entidade.

Input:
- Nome entidade, campos, rotas, serviços HTTP

Output:
- XAML Page, ViewModel, comandos de salvar/excluir, bindings, validação
- Registros no AppShell/rotas

Notas:
- Seguir MVVM e injetar serviços via DI
- Gerar testes UI (Selenium/Appium) minimal
